import { Cormorant, Lora } from "next/font/google";

export const cormorant = Cormorant({
  weight: "400",
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
});
export const lora = Lora({
  weight: "400",
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
});
